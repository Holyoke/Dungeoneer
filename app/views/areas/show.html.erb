<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @area.name %>
</p>

<p>
  <strong>Project:</strong>
  <%= @area.project.name %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag @area.floor_plan.full_map.url %>
</p>

<p>
  <strong>Filename:</strong>
  <%= image_tag @area.floor_plan.file.filename %>
</p>

<%= link_to 'Edit', edit_area_path(@area) %> |
<%= link_to 'Back', project_path(@area.project) %>
<%= button_to 'Destroy', @area, method: :delete, data: { confirm: 'Destroy area?' } %>
