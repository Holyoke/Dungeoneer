<h1>Editing Area</h1>

<p>
  <strong>Image:</strong>
  <%= image_tag @area.floor_plan.thumb.url %>
</p>

<%= render 'form', area: @area %>

<%= link_to 'Show', area_path(@area) %> |
<%= link_to 'Back', map_path(@area.map) %>
