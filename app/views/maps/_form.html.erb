<div class="flex column col-md-4">
  <%= form_for(map) do |f| %>
    <% if map.errors.any? %>
      <div class="alert alert-danger">
        <%= pluralize(map.errors.count, "error") %> prohibited this map from being saved:
        <ul>
          <% map.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-fields">
      <div>
        <%= f.label :name%>
        <%= f.text_field :name %>
      </div>

      <div>
        <%= f.label :license %>
        <%= f.text_field :license %>
      </div>

      <div>
        <%= f.label :description %>
        <%= f.text_field :description %>
      </div>

      <div class="footer">
        <div class="button"><%= submit_tag 'Save', class: "clear-formatting"%> </div>
        <div class="button"><%= link_to 'Back', maps_path, class: "clear-formatting" %></div>
      </div>
    </div>
  <% end %>
</div>
